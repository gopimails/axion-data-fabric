# OpenSky Network API Credentials (OAuth v2)
# Use CLIENT_ID and CLIENT_SECRET for authenticated access
# Leave blank to use free unauthenticated API tier
CLIENT_ID=
CLIENT_SECRET=

# Storage path for Parquet files
# Default is data/raw (relative to project root)
STORAGE_PATH=data/raw
